<%= select_tag "egreso[categoria_egreso_id]",
               options_for_select(categorias.map { |c| [c.nombre, c.id] }, selected_id),
               include_blank: "— Seleccione una categoría —",
               class: "select select-bordered w-full",
               required: true,
               name: "egreso[categoria_egreso_id]",
               id: "egreso_categoria_egreso_id" %>
