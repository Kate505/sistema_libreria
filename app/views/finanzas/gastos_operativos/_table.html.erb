<table class="table table-pin-rows px-4">
  <thead>
    <tr class="bg-base-100 z-10">
      <th>Fecha</th>
      <th>Descripción</th>
      <th class="text-right">Cantidad (C$)</th>
      <th class="text-right">Acciones</th>
    </tr>
  </thead>

  <tbody>
  <% gastos_operativos.each do |gasto| %>
    <tr class="hover:bg-base-200">

      <td class="whitespace-nowrap">
        <span class="font-semibold"><%= gasto.fecha&.strftime("%d/%m/%Y") || "—" %></span>
      </td>

      <td class="max-w-xs">
        <span class="truncate block"><%= gasto.descripcion.presence || "—" %></span>
      </td>

      <td class="text-right tabular-nums font-semibold">
        C$ <%= number_with_precision(gasto.cantidad, precision: 2, delimiter: ',') %>
      </td>

      <td class="flex gap-2 justify-end">
        <% if Current.user.can_access_menu?("ELIMINAR_GASTOS") %>
          <%= link_to finanzas_gasto_operativo_path(gasto),
                      data: { turbo_method: :delete, turbo_confirm: "¿Eliminar este gasto operativo? Esta acción no se puede deshacer." },
                      class: "btn btn-sm btn-ghost btn-square text-error",
                      title: "Eliminar" do %>
            <% inline_svg "remove.svg" %>
          <% end %>
        <% end %>
      </td>
    </tr>
  <% end %>

  <% if gastos_operativos.empty? %>
    <tr>
      <td colspan="4" class="text-center py-12 text-base-content/40">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto mb-3 opacity-30" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 11h.01M12 11h.01M15 11h.01M12 7h.01M3 5a2 2 0 012-2h14a2 2 0 012 2v14a2 2 0 01-2 2H5a2 2 0 01-2-2V5z" />
        </svg>
        <p class="text-sm">No hay gastos operativos registrados</p>
      </td>
    </tr>
  <% end %>

  </tbody>
</table>
