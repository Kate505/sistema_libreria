<table class="table table-pin-rows px-4">
  <thead>
  <tr class="bg-base-100 z-10">
    <th>SKU</th>
    <th>Nombre</th>
    <th>Marca</th>
    <th>Categoría</th>
    <th class="text-right">P. Compra</th>
    <th class="text-right">Costo Calc.</th>
    <th class="text-right">
      <span class="inline-flex items-center gap-1" title="El Precio de Venta es un precio de venta ponderado con el precio de venta anterior del producto.">
        Precio Venta
        <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 opacity-60" viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true" focusable="false">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8h.01M11 12h1v4h1m-1-10a9 9 0 1 0 0 18a9 9 0 0 0 0-18z"/>
        </svg>
      </span>
    </th>
    <th class="text-right">P. Mayor</th>
    <th class="text-right">Margen</th>
    <th>Stock</th>
    <th class="text-right">Acciones</th>
  </tr>
  </thead>

  <tbody>
  <% productos.each do |producto| %>
    <% costo_calc = producto.ultimo_costo_calculado %>
    <tr class="hover:bg-base-200">
      <td class="text-base-content/50 text-sm"><%= producto.sku %></td>

      <td class="font-medium max-w-xs">
        <span class="truncate block"><%= producto.nombre %></span>
      </td>

      <td>
        <% if producto.marca.present? %>
          <span class="badge badge-outline badge-sm"><%= producto.marca.nombre %></span>
        <% else %>
          <span class="text-base-content/30 text-sm italic">—</span>
        <% end %>
      </td>

      <td>
        <% if producto.categoria.present? %>
          <span class="badge badge-outline badge-sm"><%= producto.categoria.nombre %></span>
        <% else %>
          <span class="text-base-content/30 text-sm italic">—</span>
        <% end %>
      </td>

      <td class="text-right tabular-nums text-sm text-base-content/60">
        <% if producto.ultimo_precio_compra.to_d > 0 %>
          C$ <%= number_with_precision(producto.ultimo_precio_compra, precision: 2, delimiter: ',') %>
        <% else %>
          <span class="text-base-content/30 italic">—</span>
        <% end %>
      </td>

      <td class="text-right tabular-nums text-sm text-base-content/60">
        <% if costo_calc > 0 %>
          C$ <%= number_with_precision(costo_calc, precision: 2, delimiter: ',') %>
        <% else %>
          <span class="text-base-content/30 italic">—</span>
        <% end %>
      </td>

      <td class="text-right font-semibold tabular-nums">
        C$ <%= producto.precio_venta.to_i %>
      </td>
      <td class="text-right tabular-nums text-sm text-base-content/60">
        C$ <%= producto.precio_venta_al_mayor.to_i %>
      </td>

      <%# Margen: ((PV - costo_calc) / costo_calc) * 100 %>
      <td class="text-right tabular-nums">
        <% margen = producto.margen_estimado %>
        <% bajo = producto.margen_bajo? %>
        <span class="badge badge-sm <%= bajo ? 'badge-warning' : 'badge-success' %>">
          <%= margen %>%
        </span>
      </td>

      <td>
        <% stock = producto.stock_actual.to_i %>
        <% min = producto.stock_minimo_limite.to_i %>
        <% if stock <= 0 %>
              <span class="badge badge-error badge-sm gap-1">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                </svg>
                <%= stock %>
              </span>
        <% elsif stock <= min %>
              <span class="badge badge-warning badge-sm gap-1">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01M21 12A9 9 0 113 12a9 9 0 0118 0z"/>
                </svg>
                <%= stock %>
              </span>
        <% else %>
              <span class="badge badge-success badge-sm gap-1">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <%= stock %>
              </span>
        <% end %>
      </td>

      <td class="flex gap-2 justify-end">
        <%= link_to edit_inventario_producto_path(producto),
                    data: { turbo_frame: "producto_form" },
                    class: "btn btn-sm btn-ghost btn-square",
                    title: "Editar" do %>
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16">
            <path fill="currentColor" d="M15.502 1.94a.5.5 0 0 1 0 .706L14.459 3.69l-2-2L13.502.646a.5.5 0 0 1 .707 0l1.293 1.293zm-1.75 2.456l-2-2L4.939 9.21a.5.5 0 0 0-.121.196l-.805 2.414a.25.25 0 0 0 .316.316l2.414-.805a.5.5 0 0 0 .196-.12l6.813-6.814z"/>
            <path fill="currentColor" fill-rule="evenodd" d="M1 13.5A1.5 1.5 0 0 0 2.5 15h11a1.5 1.5 0 0 0 1.5-1.5v-6a.5.5 0 0 0-1 0v6a.5.5 0 0 1-.5.5h-11a.5.5 0 0 1-.5-.5v-11a.5.5 0 0 1 .5-.5H9a.5.5 0 0 0 0-1H2.5A1.5 1.5 0 0 0 1 2.5z"/>
          </svg>
        <% end %>

        <%= link_to inventario_producto_path(producto),
                    data: { turbo_method: :delete, turbo_confirm: "¿Eliminar el producto \"#{producto.nombre}\"? Esta acción no se puede deshacer." },
                    class: "btn btn-sm btn-ghost btn-square text-error",
                    title: "Eliminar" do %>
          <% inline_svg "remove.svg" %>
        <% end %>
      </td>
    </tr>
  <% end %>

  <% if productos.empty? %>
    <tr>
      <td colspan="11" class="text-center py-12 text-base-content/40">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto mb-3 opacity-30" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"/>
        </svg>
        <p class="text-sm">No hay productos registrados</p>
      </td>
    </tr>
  <% end %>

  </tbody>
</table>
