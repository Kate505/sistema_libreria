<turbo-frame id="usuario_form">
  <div id="gestionar_roles_modal_content">
    <%= render "gestionar_roles_usuario_modal",
               usuario: usuario,
               roles_usuario: (defined?(roles_usuario) ? roles_usuario : []),
               lista_agregar_roles: (defined?(lista_agregar_roles) ? lista_agregar_roles : []) %>
  </div>
  <%= form_with(model: usuario, url: usuario.new_record? ? seguridad_usuarios_path : seguridad_usuario_path(usuario), method: usuario.new_record? ? :post : :patch, class: 'contents') do |f| %>

    <div class="card-body">
      <h2 class="card-title text-lg">
        <%= usuario.new_record? ? "Agregar nuevo usuario" : "Actualizar usuario: #{usuario.nombre_corto}" %>
      </h2>

      <%# Errores de validación %>
      <% if usuario.errors.any? %>
        <div class="alert alert-error text-sm py-2">
          <ul class="list-disc list-inside">
            <% usuario.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">

        <div class="w-full">
          <%= f.label :email_address, 'Email', class: 'label pt-0' %>
          <%#= f.text_field :email_address, class: 'input input-bordered w-full validator', placeholder: 'Email', required: true %>
          <label class="input input-bordered validator flex items-center gap-2 w-full">
            <svg class="h-[1em] opacity-50" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
              <g stroke-linejoin="round" stroke-linecap="round" stroke-width="2.5" fill="none" stroke="currentColor">
                <rect width="20" height="16" x="2" y="4" rx="2"></rect>
                <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"></path>
              </g>
            </svg>

            <%= f.email_field :email_address,
                              class: 'w-full',
                              placeholder: 'mail@site.com',
                              required: true %>
          </label>
          <div class="validator-hint hidden">Ingrese un correo válido</div>
        </div>

        <div class="w-full relative"
             data-controller="autocomplete"
             data-autocomplete-url-value="<%= buscar_empleado_seguridad_usuarios_path %>"
             data-action="click@window->autocomplete#hide">

          <%= f.label :empleado_id, 'Empleado', class: 'label pt-0' %>

          <%= f.hidden_field :empleado_id, data: { autocomplete_target: "hidden" } %>

          <div class="relative">
            <% nombre_actual = usuario.empleado_id.present? ? usuario&.nombre_completo : "" %>

            <input type="text"
                   class="input input-bordered w-full validator pr-10"
                   placeholder="Buscar empleado..."
                   value="<%= nombre_actual %>"
                   data-autocomplete-target="input"
                   data-action="input->autocomplete#search"
                   autocomplete="off">

            <div data-autocomplete-target="loading" class="absolute right-3 top-3 hidden">
              <span class="loading loading-spinner loading-sm text-primary"></span>
            </div>
          </div>

          <ul data-autocomplete-target="results"
              class="menu dropdown-content bg-base-100 rounded-box z-[50] w-full p-2 shadow-lg absolute top-full mt-1 border border-base-200 hidden max-h-60 overflow-y-auto">
          </ul>
        </div>


        <div class="w-full flex flex-col gap-2 pb-3">
          <%= f.label :pasivo, class: 'cursor-pointer label flex items-center justify-start gap-3 w-fit' do %>
            <%= f.check_box :pasivo, class: 'checkbox' %>
            <span class="label-text">Pasivo</span>
          <% end %>

          <%= f.label :requires_password_change, class: 'cursor-pointer label flex items-center justify-start gap-3 w-fit' do %>
            <%= f.check_box :requires_password_change,
                            class: 'checkbox',
                            id: 'requires_password_change_check',
                            data: { action: 'change->password-toggle#toggle' } %>
            <span class="label-text">Contraseña temporal (requiere cambio)</span>
          <% end %>

        <% if usuario.new_record? %>
          <%# Nuevo usuario: campos de contraseña siempre visibles %>
          <div class="w-full col-span-full flex flex-col gap-3 p-3 rounded-lg bg-info/10 border border-info/30">
            <p class="text-sm text-info font-semibold">
              ℹ Ingresa la contraseña temporal que usará el usuario para su primer inicio de sesión.
              Si la dejas en blanco se usará <strong>Temporal123</strong>.
            </p>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
              <div class="w-full">
                <label class="label pt-0 text-sm">Contraseña temporal</label>
                <%= f.password_field :password,
                                     class: 'input input-bordered w-full',
                                     placeholder: 'Dejar en blanco para usar Temporal123',
                                     autocomplete: 'new-password' %>
              </div>
              <div class="w-full">
                <label class="label pt-0 text-sm">Confirmar contraseña</label>
                <%= f.password_field :password_confirmation,
                                     class: 'input input-bordered w-full',
                                     placeholder: 'Repite la contraseña',
                                     autocomplete: 'new-password' %>
              </div>
            </div>
          </div>
        <% else %>
          <%# Usuario existente: campos de contraseña ocultos hasta marcar checkbox %>
          <div id="temp_password_section"
               class="<%= usuario.requires_password_change? ? '' : 'hidden' %> col-span-full flex flex-col gap-3 p-3 rounded-lg bg-warning/10 border border-warning/30">
            <p class="text-sm text-warning font-semibold">
              ⚠ Ingresa la nueva contraseña temporal que el usuario deberá usar para iniciar sesión.
            </p>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
              <div class="w-full">
                <label class="label pt-0 text-sm">Nueva contraseña temporal</label>
                <%= f.password_field :password,
                                     class: 'input input-bordered w-full',
                                     placeholder: 'Ej: Temporal2024!',
                                     autocomplete: 'new-password',
                                     id: 'temp_password_input' %>
              </div>
              <div class="w-full">
                <label class="label pt-0 text-sm">Confirmar contraseña temporal</label>
                <%= f.password_field :password_confirmation,
                                     class: 'input input-bordered w-full',
                                     placeholder: 'Repite la contraseña',
                                     autocomplete: 'new-password',
                                     id: 'temp_password_confirm_input' %>
              </div>
            </div>
          </div>

          <script>
            (function() {
              const checkbox = document.getElementById('requires_password_change_check');
              const section  = document.getElementById('temp_password_section');
              if (!checkbox || !section) return;

              function toggle() {
                if (checkbox.checked) {
                  section.classList.remove('hidden');
                } else {
                  section.classList.add('hidden');
                  section.querySelectorAll('input[type="password"]').forEach(i => i.value = '');
                }
              }
              checkbox.addEventListener('change', toggle);
            })();
          </script>
        <% end %>

        </div><%# fin del div de checkboxes %>

        <div class="card-actions justify-between items-center sm:col-span-2 col-span-full mt-4 pt-4 border-t border-base-200">

          <div>
            <% unless usuario.new_record? %>
              <button type="button" class="btn btn-accent" onclick="my_modal_1.showModal()">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-4 mr-1">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 6a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0ZM4.501 20.118a7.5 7.5 0 0 1 14.998 0A17.933 17.933 0 0 1 12 21.75c-2.676 0-5.216-.584-7.499-1.632Z"/>
                </svg>
                Gestionar Roles
              </button>
            <% end %>
          </div>

          <div class="flex gap-2">
            <%= save_button(f) %>
            <%= cancel_button(seguridad_usuarios_path, frame_id: "usuario_form", style_class: 'btn btn-soft btn-error') %>
          </div>
        </div>

      </div>
    </div>
  <% end %>
</turbo-frame>
